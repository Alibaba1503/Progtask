<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="/css/login.css">
    <link href="https://fonts.googleapis.com/css?family=Roboto:500&display=swap" rel="stylesheet">
    <title>Login Page</title>
</head>
<body>
    <header class="header">
        <% if(emptyField == true ) { %>
            <div class="error">
                <h2 class="error-text"><strong>Oops!</strong> You didn't fill al the fields!!</h2>
            </div>
        <% } %>
        <% if(wrongUsername == true ) { %>
            <div class="error">
                <h2 class="error-text"><strong>Oops!</strong> Wrong username!!</h2>
            </div>
        <% } %>
        <% if(wrongPassword == true ) { %>
            <div class="error">
                <h2 class="error-text"><strong>Oops!</strong> Wrong password!!</h2>
            </div>
        <% } %>
    <h1 class="login-title">LOGIN PAGE</h1>
    </header>
    <form method="POST" action="/login" class="login-box">
        <input type="text" name="username" placeholder="Username..." class="login-input">
        <input type="password" name="password" placeholder="Password..." class="login-input">
        <button class="login-button" onclick="">
            <h2>LOGIN</h2>
        </button>
        <a class="login-forgot" href="">fortgot password</a>
    </form >
    <p class="account">Don't have a account yet! <a class="a-create" href="/signin">Create one</a></p>
</body>
</html>