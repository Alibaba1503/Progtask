<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="/css/signin.css">
    <link href="https://fonts.googleapis.com/css?family=Roboto:500&display=swap" rel="stylesheet">
    <title>Signin Page</title>
</head>
<body>
    <header class="header">
        <% if(valiedEmail == true ) { %>
            <div class="error">
                <h2 class="error-text"><strong>Oops!</strong> Email already used</h2>
            </div>
        <% } %>
        <% if(valiedUsername == true ) { %>
            <div class="error">
                <h2 class="error-text"><strong>Oops!</strong> Username already used</h2>
            </div>
        <% } %>
        <% if(emptyField == true ) { %>
            <div class="error">
                <h2 class="error-text"><strong>Oops!</strong> You didn't fill al the fields</h2>
            </div>
        <% } %>
        <h1 class="login-title">Signin Page</h1>
    </header>
    <form action="/signin" method="POST" class="login-box">
        <input type="text" name="firstname" placeholder="Firstname..." class="login-input">
        <input type="text" name="lastname" placeholder="Lastname..." class="login-input">
        <input type="text" name="username" placeholder="Username..." class="login-input">
        <input type="email" name="email" placeholder="Email..." class="login-input">
        <input type="password" name="password" placeholder="Password..." class="login-input">
        <button class="login-button" type="submit">
            <h2>SIGNIN</h2>
        </button>
    </form>
</body>
</html>